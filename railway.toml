# Frontend only: Root Directory = this folder. Spring Boot must use a separate service with
# Root Directory = `backend/` (see backend/railway.toml + backend/Dockerfile).

[build]
dockerfilePath = "Dockerfile"
