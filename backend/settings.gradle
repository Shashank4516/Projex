pluginManagement {
	repositories {
		gradlePluginPortal()
		mavenCentral()
	}
}

// Auto-download JDK matching java.toolchains (Railway/Railpack often only ships Java 21).
plugins {
	id 'org.gradle.toolchains.foojay-resolver-convention' version '0.10.0'
}

rootProject.name = 'projex-api'
